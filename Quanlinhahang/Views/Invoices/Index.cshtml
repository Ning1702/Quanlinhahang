@model IEnumerable<Quanlinhahang.Models.ViewModels.InvoiceRowVM>

<h2 class="mt-3">Danh sách hóa đơn</h2>

@if (TempData["msg"] != null)
{
    <div class="alert alert-info">@TempData["msg"]</div>
}

<table class="table table-bordered table-striped">
    <thead class="table-light">
        <tr>
            <th>ID</th>
            <th>Ngày lập</th>
            <th>Khách hàng</th>
            <th>SĐT</th>
            <th class="text-end">Tổng tiền</th>
            <th>Trạng thái</th>
            <th>Loại đặt bàn</th>
            <th class="text-center">Thao tác</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var x in Model)
        {
            <tr>
                <td>@x.HoaDonID</td>
                <td>@x.NgayLap.ToString("dd/MM/yyyy HH:mm")</td>
                <td>@x.KhachHang</td>
                <td>@x.SoDienThoai</td>
                <td class="text-end">@x.TongTien.ToString("#,##0")</td>

                <td>
                    @if (x.TrangThaiThanhToan == "Đã thanh toán")
                    {
                        <span class="badge bg-success">Đã thanh toán</span>
                    }
                    else if (x.TrangThaiXacNhan == "Đã xác nhận")
                    {
                        <span class="badge bg-primary">Đã xác nhận</span>
                    }
                    else
                    {
                        <span class="badge bg-warning text-dark">Chờ xác nhận</span>
                    }
                </td>

                <td>
                    @if (x.LoaiDatBan == "Offline")
                    {
                        <span class="badge bg-dark">Offline</span>
                    }
                    else
                    {
                        <span class="badge bg-info">Online</span>
                    }
                </td>

                <td class="text-center">
                    @if (x.TrangThaiThanhToan == "Đã thanh toán")
                    {
                        <a asp-action="Edit" asp-route-id="@x.HoaDonID" class="btn btn-sm btn-info">
                            <i class="bi bi-eye"></i> Xem
                        </a>
                    }
                    else
                    {
                        <a asp-action="Edit" asp-route-id="@x.HoaDonID" class="btn btn-sm btn-warning">
                            <i class="bi bi-pencil-square"></i> Sửa
                        </a>
                    }

                    @if (x.TrangThaiXacNhan == "Chờ xác nhận")
                    {
                        <form asp-action="ConfirmInvoice" asp-route-id="@x.HoaDonID" method="post" style="display:inline;">
                            <button type="submit" class="btn btn-sm btn-success">
                                <i class="bi bi-check-circle"></i> Xác nhận
                            </button>
                        </form>
                    }
                </td>
            </tr>
        }
    </tbody>
</table>
