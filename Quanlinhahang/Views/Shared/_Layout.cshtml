@* ============================================
   _Layout.cshtml - Kết hợp layout gốc + sidebar
   ============================================ *@
<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"] - Quanlinhahang</title>

    <!-- Bootstrap + CSS gốc -->
    <link rel="stylesheet" href="~/lib/bootstrap/dist/css/bootstrap.min.css" />
    <link rel="stylesheet" href="~/css/site.css" asp-append-version="true" />
    <link rel="stylesheet" href="~/Quanlinhahang.styles.css" asp-append-version="true" />

    <!-- Font Awesome (cho icon menu) -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css" />

    <style>
        /* Bố cục chính */
        body {
            overflow-x: hidden;
            font-family: "Segoe UI", sans-serif;
        }

        #wrapper {
            display: flex;
            width: 100%;
        }

        /* Sidebar trái */
        #sidebar-wrapper {
            min-width: 220px;
            max-width: 220px;
            background-color: #212529;
            color: #fff;
            transition: all 0.3s ease;
            height: 100vh;
            position: fixed;
            top: 0;
            left: 0;
            z-index: 1000;
        }

        #wrapper.toggled #sidebar-wrapper {
            margin-left: -220px;
        }

        .sidebar-heading {
            background-color: #343a40;
            font-weight: 600;
            text-transform: uppercase;
            text-align: center;
            padding: 1rem 0;
            border-bottom: 1px solid #444;
        }

        .list-group-item {
            border: none;
            background: #212529;
            color: #fff;
            transition: background 0.2s;
        }

            .list-group-item i {
                width: 20px;
                text-align: center;
                margin-right: 6px;
            }

            .list-group-item:hover {
                background-color: #495057;
            }

        /* Nội dung */
        #page-content-wrapper {
            flex-grow: 1;
            margin-left: 220px;
            transition: all 0.3s ease;
            width: 100%;
        }

        #wrapper.toggled #page-content-wrapper {
            margin-left: 0;
        }

        /* Navbar trên cùng */
        .navbar {
            z-index: 1050;
        }

        /* Responsive */
        @@media (max-width: 768px) {
            #sidebar-wrapper

        {
            margin-left: -220px;
        }

        #wrapper.toggled #sidebar-wrapper {
            margin-left: 0;
        }

        #page-content-wrapper {
            margin-left: 0;
        }

        #wrapper.toggled #page-content-wrapper {
            margin-left: 220px;
        }

        }

        /* Footer fix */
        footer {
            background-color: #f8f9fa;
            border-top: 1px solid #ddd;
            text-align: center;
            padding: 10px;
        }
    </style>
</head>

<body>
    <div id="wrapper" class="d-flex">
        <!-- Sidebar -->
        <div id="sidebar-wrapper">
            <div class="sidebar-heading">
                Quản lý nhà hàng
            </div>
            <div class="list-group list-group-flush">
                <a href="/Invoices" class="list-group-item list-group-item-action">
                    <i class="fas fa-file-invoice"></i> Hóa đơn
                </a>
                <a href="/Reports" class="list-group-item list-group-item-action">
                    <i class="fas fa-chart-line"></i> Báo cáo cá nhân
                </a>
                <a href="/TaiKhoan" class="list-group-item list-group-item-action">
                    <i class="fas fa-user"></i> Tài khoản
                </a>
            </div>
        </div>

        <!-- Phần nội dung trang -->
        <div id="page-content-wrapper">
            <header>
                <nav class="navbar navbar-expand-sm navbar-light bg-light border-bottom shadow-sm mb-3">
                    <div class="container-fluid">
                        <button class="btn btn-outline-dark me-3" id="sidebarToggle">
                            <i class="fas fa-bars"></i>
                        </button>
                        <a class="navbar-brand fw-bold" asp-area="" asp-controller="Home" asp-action="Index">Quanlinhahang</a>

                        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target=".navbar-collapse"
                                aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
                            <span class="navbar-toggler-icon"></span>
                        </button>

                        <div class="navbar-collapse collapse d-sm-inline-flex justify-content-end">
                            <ul class="navbar-nav">
                                <li class="nav-item">
                                    <a class="nav-link text-dark" asp-area="" asp-controller="Home" asp-action="Index">Trang chủ</a>
                                </li>
                                <li class="nav-item">
                                    <a class="nav-link text-dark" asp-area="" asp-controller="Home" asp-action="Privacy">Chính sách</a>
                                </li>
                            </ul>
                        </div>
                    </div>
                </nav>
            </header>

            <div class="container-fluid px-4">
                <main role="main" class="pb-3">
                    @RenderBody()
                </main>
            </div>

            <footer class="border-top mt-auto text-muted">
                <div class="container">
                    &copy; 2025 - Quanlinhahang -
                    <a asp-area="" asp-controller="Home" asp-action="Privacy">Chính sách bảo mật</a>
                </div>
            </footer>
        </div>
    </div>

    <!-- Script -->
    <script src="~/lib/jquery/dist/jquery.min.js"></script>
    <script src="~/lib/bootstrap/dist/js/bootstrap.bundle.min.js"></script>
    <script src="~/js/site.js" asp-append-version="true"></script>

    <script>
        const wrapper = document.getElementById("wrapper");
        const toggleBtn = document.getElementById("sidebarToggle");

        toggleBtn.addEventListener("click", () => {
            wrapper.classList.toggle("toggled");
        });
    </script>

    @await RenderSectionAsync("Scripts", required: false)
</body>
</html>
